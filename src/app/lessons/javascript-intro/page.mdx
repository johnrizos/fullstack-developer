export const counterHtml = `<main class="counter">
  <p>Μετρητής</p>
  <strong id="count">0</strong>
  <button id="increase">Αύξηση</button>
</main>`;

export const counterCss = `body {
  font-family: Arial, sans-serif;
  background: #f3f4f6;
  padding: 40px;
}

.counter {
  max-width: 340px;
  margin: 0 auto;
  padding: 28px;
  background: white;
  border-radius: 12px;
  text-align: center;
}

#count {
  display: block;
  margin: 18px 0;
  font-size: 48px;
  color: #2563eb;
}

button {
  background: #2563eb;
  color: white;
  padding: 10px 18px;
  border: none;
  border-radius: 8px;
  cursor: pointer;
}`;

export const counterJs = `let count = 0;

// TODO: φτιάξε function increaseCount που αυξάνει το count κατά 1
//       και ενημερώνει το textContent του #count.

// TODO: σύνδεσε click event listener στο #increase ώστε να καλεί την increaseCount.`;

export const counterSolutionJs = `let count = 0;

function increaseCount() {
  count = count + 1;
  document.querySelector("#count").textContent = count;
}

document.querySelector("#increase").addEventListener("click", increaseCount);`;

# JavaScript Basics: κάνε τη σελίδα διαδραστική

Η HTML δίνει δομή και η CSS δίνει εμφάνιση. Η **JavaScript** δίνει συμπεριφορά: τι γίνεται όταν ο χρήστης πατάει ένα κουμπί, γράφει σε ένα input ή φορτώνει δεδομένα.

Σε αυτό το μάθημα θα χτίσεις έναν μικρό counter. Είναι απλό παράδειγμα, αλλά περιέχει τρεις ιδέες που θα χρησιμοποιείς συνέχεια:

- **Variable**: κρατάμε μια τιμή στη μνήμη.
- **Function**: βάζουμε λογική σε επαναχρησιμοποιήσιμο κομμάτι.
- **Event listener**: λέμε στον browser τι να κάνει όταν συμβεί ένα event, όπως click.

## 1. Variable

Μια variable είναι ένα όνομα που κρατά μια τιμή.

```js
let count = 0;
```

Εδώ λέμε ότι ο μετρητής ξεκινά από το `0`.

## 2. Function

Μια function είναι ένα μικρό block λογικής που μπορούμε να καλέσουμε όταν το χρειαζόμαστε.

```js
function increaseCount() {
  count = count + 1;
}
```

Αυτό αυξάνει το `count` κατά ένα.

## 3. Event listener

Με ένα event listener συνδέουμε τη function με μια ενέργεια του χρήστη.

```js
document.querySelector("#increase").addEventListener("click", increaseCount);
```

Αυτό σημαίνει: βρες το κουμπί με id `increase` και όταν γίνει click, τρέξε τη function `increaseCount`.

## Mini project: interactive counter

Η αποστολή σου είναι να κρατήσεις τον counter λειτουργικό. Πάτα το κουμπί στο preview για να δεις τον αριθμό να ανεβαίνει.

Μετά δοκίμασε αλλαγές:

1. Άλλαξε το αρχικό `count` από `0` σε `5`.
2. Άλλαξε το κείμενο του button.
3. Κάνε το κουμπί πράσινο αλλάζοντας το CSS.

Το checklist ελέγχει τα βασικά που πρέπει να υπάρχουν για να δουλεύει η ιδέα.

<CodeChallenge
  title="Counter challenge"
  description="Σύνδεσε JavaScript με τη σελίδα: function, click event και ενημέρωση του DOM."
  initialHtml={counterHtml}
  initialCss={counterCss}
  initialJs={counterJs}
  solutionJs={counterSolutionJs}
  checks={[
    {
      id: "has-count-target",
      label: "Υπάρχει στοιχείο με id count.",
      type: "htmlText",
      text: "Μετρητής"
    },
    {
      id: "has-function",
      label: "Υπάρχει function increaseCount.",
      type: "jsFunction",
      name: "increaseCount"
    },
    {
      id: "has-click-listener",
      label: "Υπάρχει click event listener.",
      type: "jsIncludes",
      value: "addEventListener(\"click\""
    },
    {
      id: "updates-text",
      label: "Ο κώδικας ενημερώνει textContent.",
      type: "jsIncludes",
      value: "textContent"
    },
    {
      id: "uses-count",
      label: "Υπάρχει variable count.",
      type: "jsIncludes",
      value: "count"
    }
  ]}
/>

## Μικρός έλεγχος

<Exercise
  question="Ποιο κομμάτι κώδικα συνδέει το click του κουμπιού με τη function;"
  options={[
    "let count = 0",
    "document.querySelector('#increase').addEventListener('click', increaseCount)",
    "color: blue"
  ]}
  correctOptionIndex={1}
  explanation="Σωστά. Το addEventListener λέει στον browser ποια function να τρέξει όταν γίνει click."
/>

<Flashcards
  title="Basics — γρήγορο repaso"
  cards={[
    {
      question: "const vs let — πότε το καθένα;",
      answer: "const: default επιλογή — η μεταβλητή δεν ξανα-ανατίθεται. let: μόνο όταν η τιμή πρέπει να αλλάξει (counters, accumulators). Το var είναι παρελθόν — μην το χρησιμοποιείς."
    },
    {
      question: "Τι είναι ένα event listener;",
      answer: "Function που λες στον browser να τρέξει όταν συμβεί κάτι: element.addEventListener('click', fn). Ο browser την καλεί κάθε φορά που γίνεται το event — εσύ δεν την καλείς ποτέ απευθείας."
    },
    {
      question: "Γιατί βάζουμε λογική σε functions;",
      answer: "Επαναχρησιμοποίηση (την καλείς από πολλά σημεία), ονοματισμός (ο κώδικας διαβάζεται), και απομόνωση (αλλάζεις τη συμπεριφορά σε ΕΝΑ σημείο). Ένα click handler χωρίς function θα αντιγραφόταν παντού."
    },
    {
      question: "Τι κάνει το document.querySelector;",
      answer: "Βρίσκει το ΠΡΩΤΟ element που ταιριάζει στο CSS selector ('#id', '.class', 'button') και το επιστρέφει — ή null αν δεν υπάρχει. Από εκεί αλλάζεις textContent, classList, value κ.λπ."
    }
  ]}
/>

<TrainingLab
  title="Χτίσε counter με σκέψη state"
  scenario="Φτιάξε counter που αυξάνει, μειώνει και κάνει reset. Ο στόχος δεν είναι το κουμπί, αλλά να καταλάβεις πού ζει η τιμή και πότε αλλάζει."
  outcomes={[
    "Να ξεχωρίζεις variable, function και event handler.",
    "Να ενημερώνεις UI μετά από αλλαγή state.",
    "Να προβλέπεις τι θα εμφανιστεί πριν τρέξεις τον κώδικα."
  ]}
  steps={[
    "Δήλωσε count με αρχική τιμή 0.",
    "Γράψε render function που δείχνει το count στο DOM.",
    "Σύνδεσε τρία buttons: increment, decrement, reset.",
    "Πρόσθεσε κανόνα να μην πέφτει κάτω από 0 και εξήγησε πού μπαίνει."
  ]}
  checkpoints={[
    "Κάθε click αλλάζει πρώτα data και μετά UI.",
    "Οι functions έχουν καθαρά ονόματα.",
    "Μπορείς να εξηγήσεις τι γίνεται αν πατήσεις reset δύο φορές."
  ]}
  mistakes={[
    "Να αλλάζεις μόνο το text στο DOM και να ξεχνάς το πραγματικό state.",
    "Να γράφεις όλη τη λογική μέσα σε ένα click handler.",
    "Να μη σκέφτεσαι edge cases όπως negative count."
  ]}
  mastery="Το μάθημα έχει πετύχει όταν μπορείς να φτιάξεις μικρό interactive widget χωρίς να αντιγράφεις έτοιμο snippet."
/>
