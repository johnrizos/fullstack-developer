# Logs, Monitoring & Observability

Όταν κάτι σπάσει στην παραγωγή, δεν έχεις debugger πάνω στον χρήστη. Χρειάζεσαι observability — και η καθιερωμένη ορολογία είναι οι **τρεις πυλώνες**:

- **Logs**: τι συνέβη — διακριτά γεγονότα με context.
- **Metrics**: πόσο/πόσα — αριθμητικές χρονοσειρές (latency, error rate).
- **Traces**: πού πήγε — η διαδρομή ενός request μέσα από services.

Με αυτά απαντάς στο ερώτημα της παραγωγής: «τι έγινε, πότε, για ποιον και γιατί;» — χωρίς να αναπαράγεις το πρόβλημα τοπικά.

## Logs

Logs πρέπει να απαντούν:

- τι συνέβη,
- πότε,
- για ποιο request/user/session,
- με ποιο error.

Καλύτερα structured logs:

```json
{
  "level": "error",
  "requestId": "abc123",
  "message": "Payment failed"
}
```

Πρόσεχε να μη γράφεις secrets ή προσωπικά δεδομένα χωρίς λόγο. Καλό log:

```json
{
  "level": "warn",
  "requestId": "req_42",
  "userId": "user_123",
  "route": "/api/payments",
  "status": 502,
  "provider": "stripe"
}
```

Κακό log:

```json
{
  "apiKey": "sk_live_secret",
  "creditCard": "4242424242424242"
}
```

## Error tracking

Εργαλεία όπως Sentry βοηθούν να βλέπεις stack traces, affected users και frequency.

Error tracking δεν είναι απλό console output. Σε βοηθά να δεις:

- ποιο release έφερε το error,
- πόσοι χρήστες επηρεάζονται,
- ποιο browser/server environment το εμφανίζει,
- αν το ίδιο error επαναλαμβάνεται.

## Metrics

Metrics δείχνουν patterns:

- latency,
- error rate,
- request count,
- memory/CPU.

Τα πιο χρήσιμα production metrics για web app:

- p95 latency: πόσο αργά είναι τα πιο αργά requests,
- error rate: πόσα requests αποτυγχάνουν,
- throughput: πόσα requests ανά λεπτό,
- saturation: CPU, memory, database connections.

## Tracing

Tracing δείχνει τη διαδρομή ενός request μέσα από πολλά services. Αν ένα endpoint αργεί, trace μπορεί να δείξει αν φταίει database query, external API ή rendering.

## Alerts

Monitoring χωρίς alerts σημαίνει ότι μαθαίνεις το πρόβλημα από τους χρήστες. Alerts πρέπει να είναι λίγα και χρήσιμα:

- error rate πάνω από όριο,
- p95 latency πολύ υψηλό,
- database connections κοντά στο limit,
- background job failures,
- payment/email provider failures.

Αν ένα alert χτυπάει συνέχεια χωρίς action, η ομάδα θα αρχίσει να το αγνοεί.

## Incident notes

Μετά από production incident, κράτα σύντομο postmortem:

1. Τι συνέβη;
2. Πότε ξεκίνησε και πότε λύθηκε;
3. Πόσοι χρήστες επηρεάστηκαν;
4. Ποια ήταν η αιτία;
5. Τι αλλάζουμε για να μην ξανασυμβεί;

## Mini project: observable API route

Πάρε ένα API route και πρόσθεσε:

1. `requestId`.
2. Structured log στην αρχή και στο τέλος.
3. Error log με status code.
4. Χρόνο εκτέλεσης σε milliseconds.
5. Προσοχή να μην τυπώσεις secrets.

```ts
const startedAt = Date.now();

try {
  console.info({ requestId, route: "/api/tasks", event: "start" });
  const data = await loadTasks();
  console.info({ requestId, durationMs: Date.now() - startedAt, event: "success" });
  return Response.json({ data });
} catch (error) {
  console.error({ requestId, durationMs: Date.now() - startedAt, event: "error" });
  return Response.json({ error: "Something went wrong" }, { status: 500 });
}
```

## Πώς απαντάς στη συνέντευξη

> I think in the three pillars: structured logs with request IDs for discrete events, metrics — rate, errors, duration per endpoint — for patterns and alerting, and traces to see where a slow request actually spends its time. Error tracking like Sentry ties exceptions to releases and affected users. Alerts fire on symptoms users feel — error rate and p95 latency — not on every internal hiccup, because noisy alerts get ignored. And logs never contain secrets or card numbers; diagnosability shouldn't cost you a data leak.

<Flashcards
  title="Observability — interview drills"
  cards={[
    {
      question: "Ποιοι είναι οι τρεις πυλώνες του observability;",
      answer: "Logs: διακριτά γεγονότα με context (τι συνέβη). Metrics: αριθμητικές χρονοσειρές (πόσο συχνά, πόσο αργά) — φθηνές, ιδανικές για alerts. Traces: η διαδρομή ενός request μέσα από τα services (πού χάθηκε ο χρόνος)."
    },
    {
      question: "Τι είναι το p95 latency και γιατί όχι ο μέσος όρος;",
      answer: "Το 95% των requests είναι ταχύτερα από αυτή την τιμή. Ο μέσος όρος κρύβει τα αργά: 99 requests στα 50ms + 1 στα 5s δίνουν μ.ο. ~100ms ενώ κάποιοι χρήστες υποφέρουν. Τα percentiles (p95/p99) δείχνουν την πραγματική εμπειρία της ουράς."
    },
    {
      question: "Τι είναι η μέθοδος RED;",
      answer: "Για κάθε service παρακολουθείς: Rate (requests/sec), Errors (ποσοστό αποτυχιών), Duration (latency percentiles). Απλό, καλύπτει το 'είναι καλά το service;' — και είναι έτοιμη απάντηση στο 'τι metrics θα έβαζες;'."
    },
    {
      question: "Τι είναι το correlation/request ID;",
      answer: "Μοναδικό id που γεννιέται στην αρχή κάθε request και περνά σε ΟΛΑ τα logs και τα downstream calls (header). Έτσι ένα user report γίνεται: βρες το requestId → δες όλη τη διαδρομή του σε όλα τα services. Χωρίς αυτό, ψάχνεις βελόνα σε logs."
    },
    {
      question: "Τι κάνει το alert fatigue και πώς το αποφεύγεις;",
      answer: "Πολλά/θορυβώδη alerts → η ομάδα τα αγνοεί → χάνεται και το σοβαρό. Λύση: alerts μόνο σε ΣΥΜΠΤΩΜΑΤΑ που νιώθουν οι χρήστες (error rate, p95, failed payments) με όρια που σημαίνουν δράση, όχι σε κάθε εσωτερικό σκαμπανέβασμα."
    },
    {
      question: "Logs vs error tracking (Sentry) — γιατί και τα δύο;",
      answer: "Τα logs καταγράφουν τα πάντα γραμμικά. Το error tracking ΟΜΑΔΟΠΟΙΕΙ exceptions: ίδιο stack trace = ένα issue με μετρητή, affected users, πρώτο/τελευταίο release. Απαντά αμέσως 'νέο bug ή γνωστό; πόσους χτυπά; ποιο release το έφερε;'."
    }
  ]}
/>

<Exercise
  question="Γιατί είναι χρήσιμο το requestId στα logs;"
  options={[
    "Για να συνδέεις όλα τα logs που ανήκουν στο ίδιο request.",
    "Για να μεγαλώνει το CSS file.",
    "Για να μη χρειάζεται backend."
  ]}
  correctOptionIndex={0}
  explanation="Σωστά. Το requestId βοηθά να ακολουθήσεις ένα request μέσα στο σύστημα."
/>

<TrainingLab
  title="Βρες production bug από logs"
  scenario="Ένας χρήστης λέει ότι το checkout αποτυγχάνει. Δεν έχεις debugger. Έχεις logs, metrics και error tracking. Βρες τι χρειάζεται να έχεις καταγράψει."
  outcomes={[
    "Να γράφεις logs που βοηθούν debugging.",
    "Να ξεχωρίζεις logs, metrics, traces και alerts.",
    "Να συνδέεις user report με request id ή correlation id."
  ]}
  steps={[
    "Πρόσθεσε request id σε κάθε API request.",
    "Log method, path, status, duration και user/session id όπου επιτρέπεται.",
    "Κατέγραψε payment provider error χωρίς secrets.",
    "Ορισμός alert για αυξημένα 5xx ή checkout failures."
  ]}
  checkpoints={[
    "Μπορείς να ακολουθήσεις ένα request από frontend μέχρι backend.",
    "Τα logs δεν περιέχουν passwords/tokens.",
    "Υπάρχει metric που δείχνει αν το πρόβλημα είναι μεμονωμένο ή γενικό."
  ]}
  mistakes={[
    "Να γράφεις console.log χωρίς structure.",
    "Να μη βάζεις correlation id.",
    "Να καταγράφεις sensitive data επειδή βοηθάει στο debugging."
  ]}
  mastery="Το μάθημα έχει πετύχει όταν μπορείς να απαντήσεις 'τι έγινε, πότε, για ποιον και γιατί' από production signals."
/>
